<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.assoft.user.mapper.UserMapper">
 
    <resultMap id="BaseResultMap" type="com.assoft.user.entity.User">
        <result column="id" jdbcType="INTEGER" property="id" />
        <result column="userName" jdbcType="VARCHAR" property="userName" />
        <result column="passWord" jdbcType="VARCHAR" property="passWord" />
        <result column="realName" jdbcType="VARCHAR" property="realName" />
    </resultMap>
 
    <select id="Sel" resultType="com.assoft.user.entity.User">
        select * from user where id = #{id}
    </select>
 
	<select id="getUserList" resultType="com.assoft.user.entity.User">
        select <include refid="columns"></include> from user 
    </select>
    
    <select id="count" parameterType="com.assoft.user.entity.User" resultType="int" >
        select count(*) from user 
        <where>
    	<if test="userName != null and !&quot;&quot;.equals(userName.trim())">
	    	and userName like '%' #{userName} '%'
	    </if>
	    <if test="realName != null and !&quot;&quot;.equals(realName.trim())">
	    	and realName like '%' #{realName} '%'
	    </if>
    </where>
    </select>
    
    <select id="getUserListByPage" parameterType="java.util.Map"  resultType="com.assoft.user.entity.User">
        select * from user 
    </select>
    
    <sql id="columns">id,userName,passWord,realName</sql>
    
</mapper>